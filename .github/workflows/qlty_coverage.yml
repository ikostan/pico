---
name: Qlty Coverage Report

on:
  workflow_call:
    secrets:
      qlty_reporter_id:
        required: true

jobs:
  run:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.8"]
    steps:
      - uses: actions/checkout@main
      - name: Setup Python
        uses: actions/setup-python@main
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -r requirements.txt
          python -m pip install pytest pytest-cov
      - name: Check pytest version
        run: |
          python -m pytest --version
      - name: Check PYTHONPATH
        run: |
          pwd
          export PYTHONPATH=.:$PYTHONPATH
      - name: Check Runner Architecture
        run: uname -m
      - name: Install Qlty CLI
        run: |
          curl -L https://qlty-releases.s3.amazonaws.com/qlty/latest/qlty-x86_64-unknown-linux-gnu.tar.xz -o qlty.tar.xz || echo "Failed to download Qlty CLI tarball"
          tar -xpJf qlty.tar.xz -C . || echo "Failed to extract Qlty CLI"
          chmod +x qlty || echo "Failed to make qlty executable"
          mv qlty /usr/local/bin/ || echo "Failed to move qlty to /usr/local/bin"
          qlty --version || echo "Qlty CLI not installed correctly"
      - name: Run Tests and Generate Coverage
        # yamllint disable rule:line-length
        run: |
          python -m pytest tests/ -v --cov-report term-missing --cov=lessons --cov-report=xml
      - name: Verify Coverage Report
        run: |
          ls -l coverage.xml
          cat coverage.xml
      - name: Upload Coverage to Qlty
        env:
          QLTY_COVERAGE_TOKEN: ${{ secrets.qlty_reporter_id }}
        run: |
          qlty coverage publish --format cobertura coverage.xml --debug

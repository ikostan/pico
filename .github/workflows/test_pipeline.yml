name: Test & Lint Pipeline

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  flake8:
    name: Lint with flake8
    uses: ikostan/pico/workflows/flake8.yml@main
  pylint:
    name: Lint with pylint
    uses: ikostan/pico/workflows/pylint.yml@main
  pytest:
    name: Unitest with pytest
    uses: ikostan/pico/workflows/pytest.yml@main
